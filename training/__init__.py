"""ACT training pipeline for XDOF/ABC-130k.

Run from the repo root (the parent of this directory):
    python -m training.verify_decode --task <substring>   # do this FIRST, see README
    python -m training.train --tasks <substring> [<substring> ...]
"""
