# Works for --policy-type act, molmoact2, and pi05 -- all three are bundled
# in this pinned lerobot version (verified directly against the real public
# PyPI wheel), no fork or separate environment needed. (An earlier version
# of this file said otherwise for MolmoAct2, based on research against a
# fork branch that was never cross-checked against what's actually pinned
# below -- see README's "MolmoAct2 environment" section for the correction.)
# >=2.55: 2.49.0-2.54.0 have a Parquet-deserialization RCE (CVE-2026-41486)
ray[data,train]>=2.55
huggingface_hub
mcap
mcap-protobuf-support
# Required for --policy-type molmoact2 AND pi05 -- both real-construct
# transformers.* classes (MolmoAct2Policy/PI05Policy.__init__ call
# require_package("transformers", extra="pi"/"molmoact2")), confirmed via a
# real ImportError, not assumed. This had gone unnoticed for MolmoAct2 since
# it's only ever been exercised here via the fake-policy integration test
# pattern (which monkeypatches build_policy_and_processor and never actually
# imports the real MolmoAct2Policy class) -- a real PI05Policy.from_pretrained
# call was what actually surfaced it. Versions match lerobot==0.6.1's own
# declared "transformers-dep"/"scipy-dep" extras exactly (pip show lerobot
# --verbose, or importlib.metadata.distribution("lerobot").requires).
transformers>=5.4.0,<5.6.0
scipy>=1.14.0,<2.0.0
# Pinned <16.0.0, not left unconstrained -- lerobot==0.6.1's own "dataset"
# extra (needed by the droid ingestion strategy's
# convert_dataset_v21_to_v30.py -> lerobot.datasets -> pyav_utils.py) hard-
# requires av<16.0.0,>=15.0.0 (av.option doesn't exist in later av releases,
# confirmed against installed 18.1.0: AttributeError: module 'av' has no
# attribute 'option'). Verified 15.1.0 still works correctly for this
# project's own MCAP raw-elementary-stream decode (video_decode.py) and the
# LeRobot v3 read path (vendor/lerobot_datasource.py) -- real conversion +
# training run against real data, not just import-checked.
av>=15.0.0,<16.0.0
numpy
torch
Pillow
lerobot==0.6.1
tensorboard
pyyaml
h5py
# Not listed explicitly here on purpose -- draccus is already pulled in
# transitively by lerobot==0.6.1 (`pip show draccus` -> Required-by:
# lerobot; it's what ACTConfig/PI05Config/MolmoAct2Config are built on).
# training/train.py's --config-file now imports and calls it directly too
# (draccus.parse), not just via lerobot -- if lerobot ever stops requiring
# it, add `draccus` here explicitly rather than assuming it'll still be
# present.

# Needed by the droid ingestion strategy (training/data_prep/strategies/
# hf_lerobot_mirror.py) -- lerobot.scripts.convert_dataset_v21_to_v30 (part
# of lerobot's "dataset" extra) imports both directly; confirmed missing by
# a real ImportError/ModuleNotFoundError, not assumed.
jsonlines
datasets

# Optional: only needed if DataConfig.source_uri or --v3-root uses s3:// or
# gs:// instead of hf:// / a local path (see docs/03-prepare-data.md's
# "Data source" and "Where the converted LeRobot v3 dataset lives" sections).
# s3fs
# gcsfs

# Optional: only needed for --molmoact2-distributed-strategy fsdp2 (full
# fine-tuning at scale) -- see README's "MolmoAct2 full fine-tuning (FSDP2)"
# section. Not needed for --molmoact2-train-mode lora (the default), which
# uses plain DDP like ACT.
# accelerate>=1.13.0

# Optional: only needed for --log-perf-metrics's GPU compute-utilization %
# (torch.cuda.utilization()/memory_usage() both call into NVML under the
# hood -- confirmed via a real ModuleNotFoundError from torch itself
# without this installed). VRAM allocator stats (perf/vram_*) work without
# it; only perf/gpu_util_pct and perf/gpu_mem_util_pct need it, and
# training/perf_logging.py degrades gracefully (one warning, no crash) if
# it's missing.
# nvidia-ml-py

# Optional: only needed for --wandb (training/wandb_logging.py, via Ray's
# own ray.air.integrations.wandb.setup_wandb()) -- imported lazily inside
# train_loop.py, same pattern as nvidia-ml-py above, so it's never required
# unless --wandb is actually passed. Needs a real W&B login/API key too
# (`wandb login`), not just the package.
# wandb
